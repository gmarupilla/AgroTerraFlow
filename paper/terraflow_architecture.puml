@startuml

title TerraFlow architecture (high-level)

skinparam rectangle {
  BackgroundColor #eef2ff
  BorderColor #4f46e5
  RoundCorner 15
}

rectangle "Config\n(YAML + Pydantic)" as Config
rectangle "Ingest\n(raster, climate)" as Ingest
rectangle "Geo\n(ROI clipping)" as Geo
rectangle "Model\n(suitability score)" as Model
rectangle "Pipeline\n(orchestration)" as Pipeline
rectangle "Outputs\n(CSV, HTML map)" as Outputs

Config --> Pipeline
Pipeline --> Ingest
Pipeline --> Geo
Pipeline --> Model
Pipeline --> Outputs

Ingest --> Geo : raster
Ingest --> Model : climate summary
Geo --> Model : clipped raster values

@enduml
