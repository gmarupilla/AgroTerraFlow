@startuml
title TerraFlow pipeline steps

start
:Load YAML config;
:Validate config with Pydantic;
:Open raster dataset (rasterio);
:Open climate CSV (pandas);
:Clip raster to ROI (or fall back to full extent);
:Aggregate climate summary stats;
:Sample valid raster cells (up to max_cells);
:Compute suitability score per cell;
:Assign qualitative label (low/medium/high);
:Write results.csv;
:Optionally render interactive map (Plotly);
stop

@enduml
